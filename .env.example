# Backend
PORT=3009
NODE_ENV=development
API_PREFIX=/api/v1

# Scene agent provider (optional; unset uses the local deterministic development provider)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# CORS
ALLOWED_ORIGIN=http://localhost:5173
